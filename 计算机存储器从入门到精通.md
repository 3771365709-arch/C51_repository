# 计算机存储器从入门到精通

> 摘要：存储器是冯·诺依曼架构中"以存储器为中心"的核心部件。本文遵循"由表及里、由简到繁、由历史到前沿"的科学哲学思路，从存储器的分类与层次结构切入，深入到半导体存储元的物理本质，再上升到 Cache、虚拟存储、现代 DRAM 与新型存储器件的体系结构，力求逻辑严密、技术细节到位。

---

## 引言：存储器设计的根本矛盾

计算机存储器的设计，本质上是**容量、速度、价格**三者之间的辩证统一。清华版《计算机组成原理》指出："容量、速度和价格是存储器设计时应考虑的主要因素。由于这三者间存在相互制约的关系，没有一个存储器能同时满足这三方面的要求。"这一矛盾推动了存储器向"层次化"方向演进。

从科学技术哲学的角度看，矛盾的普遍性催生了解决矛盾的阶梯化方案：用小而快的存储器贴近 CPU，用大而慢的存储器承担海量数据，二者之间通过软硬件机制衔接，便构成了现代计算机的**存储器层次结构**（Memory Hierarchy）。

---

## 一、存储器的分类与层次结构（入门篇）

### 1.1 按在系统中的作用分类

按存储器在计算机系统中的地位与作用，可分为四类：

1. **寄存器**：位于 CPU 内部，存取速度与 CPU 匹配，容量极小，用于暂存地址、数据与中间结果。
2. **高速缓冲存储器（Cache）**：位于 CPU 与主存之间，由 SRAM 构成，存取周期在零点几纳秒级，用于缓解 CPU 与主存的速度失配。
3. **主存储器（主存/内存）**：由 DRAM 构成，存储周期数纳秒到数十纳秒，CPU 可随机访问，用于存放运行中的程序和数据。
4. **辅助存储器（辅存/外存）**：如硬盘、SSD、U 盘、光盘等，容量大、成本低、速度慢，CPU 不能直接访问，须先调入主存。

### 1.2 按存取方式分类

- **随机存取存储器（RAM）**：存取时间与物理位置无关，读写方便，主要做主存与 Cache。
- **只读存储器（ROM）**：只能随机读出，不能（或不易）写入，断电信息不丢失，用于固化系统程序（如 BIOS）。
- **串行访问存储器**：包括顺序存取（SAM，如磁带）与直接存取（DAM，如磁盘），存取时间与位置相关。
- **相联存储器（CAM）**：按内容寻址，用于快表（TLB）等。

### 1.3 按存储介质与可保存性分类

按介质可分为磁芯存储器（已淘汰）、半导体存储器（MOS 型与双极型）、磁表面存储器（磁盘、磁带）与光存储器。按断电后信息是否保留，分为**易失性**（RAM）与**非易失性**（ROM、磁盘、光盘）。按读出是否破坏原存信息，又分**破坏性读出**（DRAM）与**非破坏性读出**（SRAM、光盘）。

### 1.4 层次化结构：矛盾的阶梯化解决

现代计算机普遍采用两级存储系统：

- **Cache—主存系统**：由硬件管理，对程序员透明，目标是**提升速度**。Cache 与主存容量比约为 1:128，整个系统的等效速度接近 Cache，等效容量等于主存，单位价格接近主存。
- **主存—辅存系统**：由软硬件协同管理，构成虚拟存储器，目标是**扩充容量**。程序员可用比物理主存大得多的虚拟地址空间。

完整的层次自上而下为：**寄存器 → L1/L2/L3 Cache → 主存 → 辅存**。靠近 CPU 的层次速度更快、容量更小、单位比特价格更高；远离 CPU 的层次反之。这一金字塔结构，正是"局部性原理"得以发挥作用的物理基础。

---

## 二、半导体存储元的物理本质（进阶一）

存储器的基本单元是**存储元**（cell），它能存放一位二进制信息。存储元的物理实现，决定了整个存储器的速度、密度与成本。

### 2.1 SRAM：六晶体管双稳态触发器

SRAM 的存储元采用经典的 **6T（六晶体管）结构**。其核心是**两个交叉耦合的 CMOS 反相器**（共 4 个 MOS 管，2 个 PMOS + 2 个 NMOS）构成的双稳态触发器，另加 2 个 NMOS 访问晶体管（Pass Transistor）作为开关。

工作原理简述：

- **存储**：两个反相器形成强正反馈，节点 Q 与 Q̄ 稳定在互补电平，对应逻辑"0"和"1"。只要不断电，状态自保持，无需刷新。
- **读**：先将位线 BL、BL̄ 预充电至相等电平（通常 V_DD 或 V_DD/2），再拉高字线 WL 使访问管导通。存储节点与位线发生电荷重分配，由于位线电容远大于节点电容，位线仅产生几十毫伏的差分电压，由灵敏放大器（Sense Amplifier）检出。
- **写**：通过位线强行将 Q 拉至目标电平，反相器正反馈翻转至新状态。

6T 结构的优势是**非破坏性读出、速度极快、抗噪能力强**；代价是**每比特占用 6 个晶体管，集成度低、成本高**。故 SRAM 主要用于 CPU 内部 L1/L2 Cache。

### 2.2 DRAM：单管单电容的简约哲学

DRAM 的存储元采用 **1T1C（一个 MOS 管加一个电容）** 结构。电容上有电荷表示"1"，无电荷表示"0"。这种极简结构使 DRAM 拥有远高于 SRAM 的集成度，单位比特成本仅为 SRAM 的 1/10 至 1/20，是大容量主存的首选。

但其简约也带来两个关键问题：

1. **破坏性读出**：读出时电容对位线放电，电荷被破坏，读后必须重写（"再生"）。
2. **电荷泄漏**：电容电荷只能维持约 2ms，必须在 2ms 内对全部单元刷新一次，否则信息丢失。

### 2.3 DRAM 的刷新机制

为弥补电荷泄漏，DRAM 有三种刷新方式：

- **集中刷新**：在 2ms 末尾集中刷新所有行，期间存在"死区"，存储器不可访问。
- **分散刷新**：每个存取周期后插入一次刷新，存取周期被拉长。
- **异步刷新**：将刷新均匀分布到 2ms 内，每隔 15.6μs 刷新一行，死区时间最短，是主流方案。

此外，DRAM 普遍采用**地址复用技术**：行列地址分两次送入（行地址选通 RAS、列地址选通 CAS），使地址线引脚数减半，利于提升容量。

### 2.4 SRAM 与 DRAM 的本质对比

| 维度 | SRAM | DRAM |
|------|------|------|
| 存储元件 | 双稳态触发器 | 栅极电容 |
| 晶体管数/比特 | 6 | 1 |
| 是否需刷新 | 否 | 是（2ms 内） |
| 读出性质 | 非破坏性 | 破坏性，需重写 |
| 速度 | 快（纳秒级） | 慢 |
| 集成度 | 低 | 高 |
| 成本/比特 | 高 | 低 |
| 典型用途 | Cache | 主存 |

这种"以复杂换速度"与"以简约换密度"的取舍，体现了工程哲学中"没有银弹"的深刻道理：任何技术选择都是在约束条件下对矛盾双方的具体扬弃。

---

## 三、主存组织与编址（进阶二）

### 3.1 主存的基本结构

主存由三部分组成：**存储体**、**地址寄存器（MAR）** 与 **数据寄存器（MDR）**。

- 存储体由大量存储元阵列构成，每个存储单元存放一个存储字，位数称为字长。
- MAR 用于暂存待访问地址，其位数决定了可寻址单元数量（n 位 MAR 可寻址 2ⁿ 单元）。
- MDR 用于暂存读出或写入的数据，位数等于存储字长。

### 3.2 地址译码与芯片扩展

存储芯片内部通过行、列译码器选中具体存储单元。当单片容量不足时，采用**位扩展**（并联芯片增加字长）与**字扩展**（片选逻辑增加存储字数）组合实现容量扩展。例如 8K×8 位芯片扩展为 32K×16 位，需字扩展 4 倍、位扩展 2 倍，共 8 片。

经典芯片如 Intel 2114（1K×4 位 SRAM）即为该结构的典型实例，其行译码 6-64，列译码 4-16，是教学与工程实践中理解存储阵列组织的重要样板。

---

## 四、Cache 存储系统（精通一）

### 4.1 局部性原理：Cache 的哲学根基

Cache 之所以有效，根植于程序行为的**局部性原理**（Principle of Locality）：

- **时间局部性**：近期访问过的数据很可能被再次访问（如循环变量）。
- **空间局部性**：访问往往聚集在相邻地址（如数组、指令顺序执行）。
- **顺序局部性**：指令按顺序执行的概率高。

正是局部性，使"小容量 Cache 命中大部分访问"成为可能。命中率（hit rate）与命中时间（hit time）、缺失代价（miss penalty）共同决定 Cache 性能。

### 4.2 映射方式

Cache 与主存之间的地址映射有三种基本方式：

1. **直接映射**：主存块 i 映射到 Cache 块 i mod N（N 为 Cache 块数）。实现简单、速度快，但冲突率高。
2. **全相联映射**：主存块可映射到任意 Cache 块。命中率最高，但查找复杂、成本高。
3. **组相联映射**：Cache 分组，主存块映射到固定组的任意块。是前两者的折中，现代 L1 多用 2~8 路组相联。

### 4.3 替换算法与写策略

当 Cache 满时需替换：常见算法有 **LRU**（最近最少使用）、**FIFO**、**随机** 与 **LFU**，其中 LRU 在实际中命中率最优但实现复杂。

写策略解决 Cache 与主存一致性问题：

- **全写法（Write-Through）**：写时同时更新 Cache 与主存，简单但慢。
- **写回法（Write-Back）**：只更新 Cache，被替换时再写回主存，快但需脏位标记。
- 对写缺失，又有**写分配**（调入后再写）与**非写分配**（直接写主存）之分。

### 4.4 多级 Cache

现代 CPU 普遍采用 L1（小而快，分离指令/数据）、L2（中等）、L3（大而共享）的多级结构。L1 追求最低延迟，L3 追求最高命中率，二者分工使整体性价比最优。

---

## 五、虚拟存储系统（精通二）

### 5.1 虚拟存储的提出

主存—辅存系统通过软硬件协同，为程序员提供比物理内存大得多的**虚拟地址空间**。它解决的是"容量"问题，与 Cache 解决"速度"问题相呼应，构成存储层次的两条主线。

### 5.2 三种管理方式

- **页式**：主存与辅存均划分为固定大小的页（如 4KB），通过页表映射。无外碎片，但有内碎片，是现代主流。
- **段式**：按程序逻辑结构分段，段长可变。便于共享与保护，但易产生外碎片。
- **段页式**：先分段再分页，兼顾二者优点，代价是地址变换更复杂。

### 5.3 TLB 与多级页表

页表常驻内存使访存变慢，故引入 **TLB（Translation Lookaside Buffer，快表）**，本质是相联存储器（CAM），缓存近期页表项，使地址变换与 Cache 访问可并行进行。对大地址空间，采用**多级页表**以节省页表存储。

Cache 与虚拟存储协同工作，形成从寄存器到辅存的完整数据通路，是现代计算机"以存储器为中心"思想的物质体现。

---

## 六、辅助存储与现代技术演进（精通三）

### 6.1 磁表面与光存储

辅存早期以**磁带、磁盘**为主。磁表面存储利用磁性材料的剩磁极性记录"0/1"。磁盘存储器采用直接存取（DAM），由磁道、柱面、扇区组织数据。**RAID（磁盘冗余阵列）** 通过条带化与镜像/校验，在容量、速度与可靠性间取得平衡。光存储器利用激光读写，曾是大容量分发的主流，现渐被闪存取代。

### 6.2 固态硬盘与 NAND 闪存

**SSD** 以 NAND 闪存为核心。按存储单元每位存储的比特数分为 SLC、MLC、TLC、QLC，容量递增、可靠性递减。NAND 的"先擦后写"和有限擦写次数催生了 **FTL（闪存转换层）**、磨损均衡与垃圾回收等机制。3D NAND 通过垂直堆叠层数（已达 321 层）大幅提升密度，是当代主流。

### 6.3 现代 DRAM：DDR5 与 HBM

- **DDR5**：采用 1c（10nm 第六代）工艺，速度与能效较 DDR4 显著提升，是当代服务器与 PC 主存主流。MRDIMM 通过同时操作两个 Rank 进一步提速。
- **HBM（High Bandwidth Memory）**：通过 **TSV（硅通孔）** 垂直堆叠多层 DRAM 芯片，配以超宽接口。JEDEC 于 2025 年发布 **HBM4 标准（JESD270-4）**，采用 2048 位接口、传输速率 8GB/s、总带宽突破 2TB/s，较上一代提升约 33%，能效提升逾 40%。SK 海力士于 2025 年 9 月全球首发 12 层 HBM4 并建立量产体系，成为 AI 训练 GPU（如 NVIDIA GB300）的核心配套。

### 6.4 CXL 与分层内存

随着 AI 大模型对内存"吞噬式"需求爆发，传统 CPU-DRAM-块存储两级架构已不敷使用，业界演进为**多层次内存与存储体系**：以 CXL（Compute Express Link）协议为基础，将 DRAM 资源池化、可组合化部署于 CPU/GPU 与块存储之间，并引入 HBF（高带宽闪存）与 GPU-Direct 闪存等中间层，在延迟、带宽、容量、功耗间精细平衡。这正是存储层次哲学在 AI 时代的延续与再创造。

---

## 七、总结与展望

纵观计算机存储器的发展史，始终贯穿着**"容量—速度—成本"三角矛盾**的辩证运动：

- **微观上**，SRAM 的 6T 双稳态触发器与 DRAM 的 1T1C 电容，是"稳定与密度"两种技术路线的对立统一。
- **中观上**，Cache—主存—辅存的金字塔层次，是局部性原理对硬件不可能三角的工程化求解。
- **宏观上**，从磁芯到 DDR5，从 HDD 到 3D NAND，从 DRAM 到 HBM4 与 CXL 分层内存，每一次技术跃迁都是矛盾在新条件下的再平衡。

展望未来，存算一体（PIM/AiM）、新型非易失性存储（MRAM、ReRAM、PCM）、光互连内存等正在打破冯·诺依曼架构的"存储墙"。但无论形态如何变迁，其底层哲学——在有限物理条件下，以层次化与抽象化手段逼近无限需求——将始终是计算机存储器发展的方法论内核。

> 理解存储器，不仅要记住"SRAM 快而贵、DRAM 慢而廉"的结论，更要追问"为什么"——从晶体管到体系结构，从电荷泄漏到虚拟页表，每一步都是工程对物理极限的精确回应。这正是"从入门到精通"的真正含义。

---

## 参考资料

1. 清华大学出版社《计算机组成原理》第 3 章 存储器层次结构.
2. 清华大学出版社《微型计算机原理与接口技术（第 4 版）》第 5 章 存储器系统.
3. Pennsylvania State University, *Chapter 6: Memory*（CS 组成原理课件）.
4. SK hynix Newsroom, *SK hynix at SC25: Showcasing Advanced AI Memory*（2025-11-28）.
5. SK hynix Newsroom, *SK hynix Showcases Full-Stack AI Memory Portfolio at 2025 OCP Global Summit*（2025-10-31）.
6. JEDEC, *JESD270-4 HBM4 标准*（2025-04 发布）.
7. Micron 投资者关系，AI 专用 SSD 产品组合发布稿.
8. 环球老虎财经，《存储芯片"超级周期"，真的来了？》（2025-11-02）.
9. 腾讯云开发者社区，《内存焦虑？长文梳理：HBM/DRAM/CXL/HBF/GPU-Direct》（2025-10-09）.
